version: '3.8'
services:
  bot:
    build: .
    env_file:
      - .env
    restart: unless-stopped
    container_name: instabot
    volumes:
      - ./sessions:/app/sessions:rw
      - ./downloads:/app/downloads:rw
